services:
  - type: web
    name: vat-filer
    env: python
    region: frankfurt
    buildCommand: |
      pip install --upgrade pip setuptools wheel
      pip install -r requirements.txt
      npm ci
      npm run build
      rm -rf static/app
      mkdir -p static/app
      cp -r dist/* static/app/
    startCommand: python -m uvicorn main:app --host 0.0.0.0 --port 
    autoDeploy: true
    envVars:
      - key: BASE_URL
        value: https://test-api.service.hmrc.gov.uk
      - key: HMRC_REDIRECT_URI
        value: https://YOUR-RENDER-URL.onrender.com/oauth/hmrc/callback
      - key: HMRC_CLIENT_ID
        sync: false
      - key: HMRC_CLIENT_SECRET
        sync: false
      - key: SESSION_SECRET
        generateValue: true
      - key: DATA_DIR
        value: .
